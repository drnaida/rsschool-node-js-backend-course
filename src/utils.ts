export const parseInputCommand = (data) => {
    const command = data.toString().split(' ')[0];
    const value = data.toString().split(' ')[1];

    if (command == 'mouse_up') {
        
    }
}